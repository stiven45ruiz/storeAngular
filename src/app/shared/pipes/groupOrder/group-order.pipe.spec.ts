import { GroupOrderPipe } from './group-order.pipe';

describe('GroupOrderPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupOrderPipe();
    expect(pipe).toBeTruthy();
  });
});
